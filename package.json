{
  "name": "mochi-cast",
  "version": "0.1.0",
  "private": true,
  "description": "麻薯投屏 - Open source browser extension for DLNA casting to smart TVs",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @mochi-cast/extension dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.0"
}
